apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-game-engine-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-game-engine-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-game-engine-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
